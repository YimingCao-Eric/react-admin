/**
 * Role Model
 * 
 * Represents a user role in the system.
 * Roles are used to define user permissions and access levels.
 */
export class Role {
    constructor(
        public id = 0,
        public name = '') { }
}